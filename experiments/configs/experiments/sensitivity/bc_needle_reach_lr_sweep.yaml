# Hyperparameter sensitivity - Learning rate sweep for BC on Needle Reach
base_config: "../base/bc_base.yaml"

experiments:
  - name: "bc_nr_lr_1e-5"
    training:
      needle_reach:
        learning_rate: 1.0e-5
  
  - name: "bc_nr_lr_5e-5"
    training:
      needle_reach:
        learning_rate: 5.0e-5
  
  - name: "bc_nr_lr_1e-4"
    training:
      needle_reach:
        learning_rate: 1.0e-4
  
  - name: "bc_nr_lr_5e-4"
    training:
      needle_reach:
        learning_rate: 5.0e-4
  
  - name: "bc_nr_lr_1e-3"
    training:
      needle_reach:
        learning_rate: 1.0e-3